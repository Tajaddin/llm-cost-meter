global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "llm-cost-meter"
    static_configs:
      - targets: ["host.docker.internal:9000"]
        labels:
          app: "llm-cost-meter-demo"
